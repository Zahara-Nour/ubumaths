/**
 * Pedagogical Differentiation — Rules Barrel
 *
 * Aggregates the per-family pedagogical rule modules. Each family file
 * (`triviae.ts`, `linearity.ts`, `power.ts`, `product-quotient.ts`,
 * `trigonometric.ts`, `exponential-logarithm.ts`, `radical.ts`,
 * `hyperbolic.ts`) exports a small set of helper functions consumed by the
 * dispatcher in `pipeline.ts`.
 *
 * Phase 1 placeholder — filled progressively in Phase 2a–2f.
 *
 * @module mathAST/pedagogical-differentiation/pedagogical-rules
 */

export {};
