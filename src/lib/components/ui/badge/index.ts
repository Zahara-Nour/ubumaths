export { default as Badge } from './badge.svelte';

// Re-export types from dedicated types file for better TypeScript compatibility
export type { BadgeVariant } from './types.js';
// Note: badgeVariants is exported from badge.svelte and can be imported directly if needed
