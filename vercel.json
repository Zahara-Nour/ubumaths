{
	"crons": [],
	"ignoreCommand": "if [ \"$VERCEL_ENV\" != \"production\" ]; then exit 0; else git diff --quiet HEAD^ HEAD -- ':(exclude)docs/**' ':(exclude)**/*.md'; fi",
	"headers": [
		{
			"source": "/upsilon-simulator/(.*)",
			"headers": [
				{
					"key": "Cache-Control",
					"value": "public, max-age=2592000"
				}
			]
		}
	]
}
